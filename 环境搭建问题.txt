oracle10g数据库
oraparam.ini取消注释，加上6.1
显卡不要用英伟达的，这个垃圾安装UI不支持英伟达
http://www.th7.cn/db/Oracle/201603/179108.shtml

oracle kaiqiw945

远程的话：客户端连一个域里的地址
odac+plsql

F:\oracle\network\ADMIN里的2个文件真几把蛋疼

SID_LIST_LISTENER =
  (SID_LIST =
    (SID_DESC =
      (GLOBAL_DBNAME=ORCL)
      (ORACLE_HOME = F:\oracle)
      (SID_NAME=ORCL)
    )
  )


LISTENER =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC1521))
      (ADDRESS = (PROTOCOL = TCP)(HOST = 192.168.0.100)(PORT = 1521))
    )
  )
-------------------

ORCL =
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = 192.168.0.100)(PORT = 1521))
    (CONNECT_DATA =
      (SERVER = DEDICATED)
      (SERVICE_NAME = orcl)
    )
  )
重启服务ok了，
create user xxx identified by xxx
grant dba to xxx
-----------
plsql:
服务里自动开启，但是没有网好像不能连上数据库
要联网
要改2个文件的ip地址成本机，端口是1521



iis
控制面板的程序里面启用


js移动开发：(发现很多框架，你搜组合就能搜到！！github真开放)
sdk，文件拖过来就行
jdk,不需要安装，配置好环境变量就行，切记，不要多配，也不要多加分号
ionic，cordova：需要改掉npm的registery地址
学到的指令：
where ionic
ionic serve
npm config lisy -l
还有更新npm，更新其他模块，先复制文件，在install，和build（cmd还是不行）
npm的细节：安装在program里，其他的插件都在appdata漫游数据里，会有cmd文件（dos使用）


npm:菜鸟教程

ionic   serve

cordova platform add/remove  android/ios
cordova plugin add/remove  cordova-plugin-camera @2.3.0   =>直接git下载也行

cordova需要java环境=》jdk=》环境变量=》安卓api的java包
ios就需要导入ios的环境包=》工具=》打包ios！（签名+ios版本+设备类型！（真实设备/虚拟设备））


dos:
path配置命令
%%就是路径别名
调用api的路径

shift+右键
路径直接把文件丢进dos！
control+c退出


java就装c盘算了，其他的容易javac不能命令


项目构建，资源下载压缩工具：
先有了bower=>基于这个出现了  guilp,gradle,grunt还有 
docker没用过，应该是部署利器




GIT:
bash可以执行命令
GUI是命令封装的一层界面
乌龟更好的界面（不需要什么配置，只要下载的地址是对的就行）！